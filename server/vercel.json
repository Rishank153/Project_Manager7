{
  "installCommand": "npm install --legacy-peer-deps",  // Fixed typo in "legacy"
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "server.js"
    }
  ]
}
